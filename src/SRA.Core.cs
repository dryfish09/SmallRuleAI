using System;
namespace SRA
{
	class Core
	{
		static void Logic
		{
			bool run = true;
			while(run)
			{
				// for null safety
				string input = Console.ReadLine()?.ToLower?.Trim() ?? "";
				// logic, now i don't have times
				// todo: add logic below:
			}
		}
	}
}
